plugin: aws_ec2
regions:
  - us-east-1

filters:
  tag:role: webserver  

hostnames:
  - private-ip-address

compose:
  ansible_user: "'ubuntu'"
  ansible_ssh_private_key_file: "'/home/ubuntu/.ssh/prod.pem'"
